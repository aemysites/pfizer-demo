/**
 * adds carousel scrolling behavior to a parent element
 *
 * @param {HTMLElement} target
 */
export default function decorateCarouselScroller(target) {
  target.classList.add('core-carousel-scroller');
}
