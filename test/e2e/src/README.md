# Directory structure

## Testing group

The tests from the one testing group should be defined in one _*.spec.js_ file.

## Testing group tests

Each test should be defined into own _*.test.js_ file and placed into directory that
named according to the testing group name.
